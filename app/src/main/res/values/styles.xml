<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="SplashTheme" parent="AppTheme">
        <item name="android:statusBarColor">@color/colorPrimaryBackground</item>
        <item name="android:windowBackground">@drawable/background_splash</item>
    </style>
    <!-- windowBackground, statusBarColor -->
    <!-- @drawable/background_splash, @color/colorPrimaryBackground -->

    <style name="DetailInfoTheme" parent="AppTheme">
        <item name="android:statusBarColor">@color/colorCardBackground</item>
    </style>

</resources>
